//function that takes a number parameter and multiplies it by 2
function multiplyByTwo(num: number): number {
    return num * 2;
}

// Example usage
const result: number = multiplyByTwo(8);  

//result to the console
console.log(`${result}`);
